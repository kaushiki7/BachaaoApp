<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bachao">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CommonFC"></activity>
        <activity android:name=".JoinUs" />
        <activity android:name=".Medicine" />
        <activity android:name=".SafetyList" />
        <activity android:name=".ContactUs" />
        <activity android:name=".StaySafe" />
        <activity android:name=".AboutUs" />
        <activity android:name=".UserDetails" />
        <activity android:name=".NgoDetails" />
        <activity android:name=".More" />
        <activity android:name=".Donate" />
        <activity android:name=".DisasterType" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>